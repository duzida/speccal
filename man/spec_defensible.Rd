% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_defensible.R
\name{spec_defensible}
\alias{spec_defensible}
\title{Data-driven definition of defensible specifications}
\usage{
spec_defensible(
  null,
  threshold = 0.1,
  sensitivity = c(0.075, 0.15),
  alpha = 0.05,
  within = NULL,
  axes = NULL
)
}
\arguments{
\item{null}{A \code{\link[=spec_null]{spec_null()}} object.}

\item{threshold}{False-positive rate above which a level is not defensible
(set before looking at the observed results; default 0.10).}

\item{sensitivity}{Additional thresholds reported for sensitivity.}

\item{alpha}{Significance level used to count a false positive.}

\item{within}{Optional named list, e.g. \code{list(covset = c("socio", "full"))}:
all axes other than the named one are assessed only within these levels
(typically the adequately adjusted covariate sets), so that the behaviour
of a definition or coding is separated from residual confounding. The named
axis itself is assessed across all specifications.}

\item{axes}{Axis columns to assess (default: all axis columns in the null).}
}
\value{
An object of class \code{spec_defensible}: a data frame with \code{axis},
\code{level}, \code{n_specs} (per replicate), \code{fpr}, \code{se}, \code{lo}, \code{hi}, \code{defensible}
and one logical column per sensitivity threshold; attributes \code{threshold},
\code{alpha}, \code{within}, \code{restrict}.
}
\description{
Measures, for every level of every analytic choice, the false-positive rate
under a permutation null (the share of null specifications with
\code{p < alpha}). A level whose rate exceeds a prespecified threshold is classed
as not defensible. Because specifications within one permutation are
correlated, the confidence interval of each rate is taken from the variation
of the per-replicate rate across permutations, not from a binomial formula.
}
\details{
The criterion detects levels that fail to control confounding (with a
Freedman-Lane null) and outcome definitions that saturate; it cannot detect
over-adjustment, because adjusting for a collider or mediator does not
create false positives when there is no association.
}
\seealso{
\code{\link[=spec_keep]{spec_keep()}} to translate the classification into a logical
vector over a \code{spec_grid}.
}
