% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_axes.R
\name{spec_axes}
\alias{spec_axes}
\title{Define the axes of a specification grid}
\usage{
spec_axes(
  outcome,
  covariates,
  coding = c("per_sd", "log2", "q4_vs_q1"),
  weighting = c("design", "unweighted"),
  ...
)
}
\arguments{
\item{outcome}{Named character vector or list: level name -> outcome variable
name in \code{data} (binary 0/1).}

\item{covariates}{Named list: level name -> character vector of covariate
names (\code{character(0)} for a crude model).}

\item{coding}{Exposure parameterisation. Either a character vector of built-in
codings (\code{"per_sd"}, \code{"log2"}, \code{"q4_vs_q1"}, \code{"raw"}) or a named list of
\code{list(fun = function(v) ..., contrast = "shift" | "binary")}. \code{fun} maps the
raw exposure to the analysed variable (computed on the full sample before
any subsetting); \code{contrast} tells the marginal-effect routine whether the
effect is a one-unit shift or a 0/1 contrast.}

\item{weighting}{Character vector with any of \code{"design"} (survey-weighted,
design-based variance) and \code{"unweighted"} (ordinary logistic regression).}

\item{...}{Additional subset axes, each a named list of quoted expressions
(or logical vectors) evaluated in \code{data}, e.g.
\code{sample = list(all = TRUE, age40 = quote(Age >= 40))},
\code{missing = list(imputed = TRUE, complete = quote(complete_case == 1))}.
Subsets are applied as survey domains (\code{subset()} on the design), never by
filtering the data before the design is built.}
}
\value{
An object of class \code{spec_axes}.
}
\description{
An axis is one analytic choice with two or more levels. The grid is the full
cross of all axes. Every level must be something a reasonable analyst could
have chosen; the grid does not endorse levels, it measures their consequence.
}
\examples{
ax <- spec_axes(
  outcome    = c(any = "y_any", severe = "y_severe"),
  covariates = list(crude = character(0), demo = c("Age", "Gender")),
  coding     = c("per_sd", "log2"),
  sample     = list(all = TRUE, age40 = quote(Age >= 40)))
ax
}
