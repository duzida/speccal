% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/breakpoint_boot.R
\name{breakpoint_boot}
\alias{breakpoint_boot}
\title{Breakpoint of a two-segment model with bootstrap boundary diagnostics}
\usage{
breakpoint_boot(
  data,
  design,
  exposure,
  outcome,
  covariates = character(0),
  transform = log2,
  grid = c(0.05, 0.95, 41),
  B = 200L,
  resampling = c("rao-wu", "naive"),
  seed = 1L,
  cores = 1L,
  sensitivity_grid = NULL
)
}
\arguments{
\item{data}{Data frame.}

\item{design}{A \code{survey.design} on \code{data}.}

\item{exposure, outcome}{Column names.}

\item{covariates}{Character vector of covariate names.}

\item{transform}{Transformation of the exposure (default \code{log2}).}

\item{grid}{\verb{c(lower percentile, upper percentile, number of candidates)}.}

\item{B}{Number of bootstrap replicates.}

\item{resampling}{\code{"rao-wu"} or \code{"naive"}.}

\item{seed}{Integer seed (\code{set.seed(seed)} before the Rao-Wu replicate
weights are drawn; \code{set.seed(seed + b)} before naive replicate \code{b}).}

\item{cores}{Cores for \code{parallel::mclapply} over replicates.}

\item{sensitivity_grid}{Optional second grid, e.g. \code{c(0.15, 0.85, 25)}, on
which the analysis is repeated to show how the interval moves with the
search range.}
}
\value{
A list of class \code{breakpoint_boot}: \code{observed} (breakpoint on the
original scale, whether it lies on a bound, deviance improvement over the
linear model and the P value of the slope change), \code{boot} (one row per
replicate: \code{bp}, \code{at_lo}, \code{at_hi}, \code{ddev}), \code{summary} (percentile
interval, shares at the bounds, share of replicates in which the
two-segment model improved on the linear one), \code{grid} (candidate values on
the original scale), and the same for \code{sensitivity} when requested.
}
\description{
Fits a two-segment (broken-stick) logistic model on the transformed
exposure, choosing the breakpoint by minimum deviance over a fixed grid of
candidates at equally spaced percentiles of the exposure in the analysis
sample. The bootstrap distribution of the selected breakpoint is reported
together with the share of replicates that selected the first or last
candidate: a breakpoint whose interval is set by the search range rather
than by the data piles up at the bounds. Two resampling schemes are
available: \code{"rao-wu"} (Rao-Wu rescaled bootstrap of primary sampling units
within strata via \code{survey::as.svrepdesign(type = "subbootstrap")}, fitted by
weighted maximum likelihood) and \code{"naive"} (primary sampling units resampled
with replacement within strata without rescaling, refitted with
\code{survey::svyglm}; understates the variance when strata contain few units,
provided for comparison).
}
