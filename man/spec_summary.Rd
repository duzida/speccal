% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_summary.R
\name{spec_summary}
\alias{spec_summary}
\title{Calibrated summary of a specification curve}
\usage{
spec_summary(
  grid,
  null = NULL,
  keep = NULL,
  alpha = 0.05,
  direction = c("modal", "positive", "negative")
)
}
\arguments{
\item{grid}{A \code{spec_grid}.}

\item{null}{Optional \code{\link[=spec_null]{spec_null()}} computed on the same grid (or a subgrid).}

\item{keep}{Optional logical vector over the rows of \code{grid} selecting the
specifications to summarise (for example from \code{\link[=spec_keep]{spec_keep()}}). Rows not
selected are ignored; with a null, the null is restricted to the same
specifications.}

\item{alpha}{Significance level.}

\item{direction}{\code{"modal"} (majority sign of the estimates among the
summarised specifications), \code{"positive"} or \code{"negative"}.}
}
\value{
A data frame of class \code{spec_summary}, one row per exposure.
}
\description{
For each exposure: the share of specifications significant on the odds-ratio
scale (and on the marginal risk-ratio and risk-difference scales when
available), the modal direction and the share of estimates on that side,
the share significant in the modal direction, and the vibration-of-effects
summaries (1st and 99th percentile odds ratios, relative odds ratio,
relative P value, Janus effect). With a \code{\link[=spec_null]{spec_null()}}, the share significant
in the modal direction is compared with its null distribution (permutation
P value), and the effective number of independent specifications is
estimated in two ways: from the variance of the per-replicate share of
significant specifications under the null (\code{n_eff_var = p(1 - p) / Var})
and from the eigenvalues of the correlation matrix of the estimates across
replicates (Li and Ji, 2005). An approximate interval for the observed share
uses \code{n_eff_var} in place of the nominal denominator; the permutation P value
remains the formal test.
}
\references{
Li J, Ji L. Adjusting multiple testing in multilocus analyses
using the eigenvalues of a correlation matrix. Heredity 2005;95:221-227.
}
