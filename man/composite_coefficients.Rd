% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composite_constraint.R
\name{composite_coefficients}
\alias{composite_coefficients}
\title{Constituent coefficients: imposed by the composite versus estimated freely}
\usage{
composite_coefficients(
  data,
  design,
  composite,
  constituents,
  outcome,
  covariates = character(0),
  transform = log,
  subset = NULL
)
}
\arguments{
\item{data}{Data frame.}

\item{design}{A \code{survey.design} on \code{data} (row order must match).}

\item{composite}{Name of the composite column.}

\item{constituents}{Named numeric vector of constituent column names and
their weights on the transformed scale, e.g. \code{c(NEU = 1, LYM = -1)}.
The identity \code{transform(composite) == sum(w * transform(constituent))} is
checked.}

\item{outcome}{Outcome column name.}

\item{covariates}{Character vector of covariate names.}

\item{transform}{Transformation under which the composite is linear in its
constituents (default \code{log}).}

\item{subset}{Optional logical vector selecting the analysis domain.}
}
\value{
A data frame with one row per constituent.
}
\description{
Fits, for one specification, the model with the composite alone and the
model with all constituents entered freely, and returns for each constituent
the coefficient the composite implies (its coefficient times the fixed
weight) and the freely estimated coefficient, with standard errors.
}
