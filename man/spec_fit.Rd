% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_fit.R
\name{spec_fit}
\alias{spec_fit}
\title{Fit every specification of a grid}
\usage{
spec_fit(
  axes,
  data,
  design = NULL,
  exposure,
  marginal = TRUE,
  cores = 1L,
  quiet = FALSE
)
}
\arguments{
\item{axes}{A \code{\link[=spec_axes]{spec_axes()}} object.}

\item{data}{Data frame containing outcomes, covariates, exposures and design
variables.}

\item{design}{A \code{survey.design} object built on the full \code{data} (row order
must match), or \code{NULL} when only unweighted specifications are requested.}

\item{exposure}{Character vector of exposure column names.}

\item{marginal}{Logical; compute marginal RD and RR (default \code{TRUE}).}

\item{cores}{Number of cores for \code{parallel::mclapply} over exposure x coding
(set BLAS threads to 1 when \code{cores > 1}).}

\item{quiet}{Suppress progress messages.}
}
\value{
A data frame of class \code{spec_grid} with one row per specification and
exposure, columns \code{exposure}, the axis columns, \code{estimate} (log odds ratio),
\code{se}, \code{or}, \code{lo}, \code{hi}, \code{p}, \code{rd}, \code{rd_se}, \code{p_rd}, \code{rr}, \code{lrr_se}, \code{p_rr},
\code{base_risk}, \code{prevalence}, \code{n}, \code{design_df}, \code{converged}, \code{iter},
\code{max_abs_b}, \code{max_abs_se}, \code{sep_flag}. Attributes \code{axes}, \code{exposure},
\code{refit} (a function \code{refit(data)} that repeats the fit on new data with the
same design structure; used by \code{\link[=spec_null]{spec_null()}}).
}
\description{
Fits one logistic model per specification and exposure. Survey-weighted
specifications use \code{survey::svyglm} on the full design with subsets applied
as domains (\code{subset(design, keep)}), so that design-based variances are
correct for restricted samples. Every fit records convergence, iteration
count, the largest absolute coefficient and standard error, and a
quasi-separation flag (either exceeding 10). Marginal risk differences and
risk ratios are obtained by standardisation over the domain with delta-method
variances.
}
