% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_null.R
\name{spec_null}
\alias{spec_null}
\title{Permutation null distribution for a specification grid}
\usage{
spec_null(
  grid,
  scheme = c("fl", "simple"),
  B = 300L,
  reps = NULL,
  stratum = NULL,
  covariates = NULL,
  transform = log,
  inverse = exp,
  restrict = NULL,
  seed = 1L,
  dir = NULL,
  cores = 1L,
  data = NULL,
  keep = c("estimate", "p", "rr", "p_rr", "sep_flag")
)
}
\arguments{
\item{grid}{A \code{spec_grid} produced by \code{\link[=spec_fit]{spec_fit()}} (or by \code{\link[=as_spec_grid]{as_spec_grid()}}
with a \code{refit} function and \code{data}).}

\item{scheme}{\code{"fl"} or \code{"simple"}.}

\item{B}{Number of permutations; ignored when \code{reps} is given.}

\item{reps}{Integer vector of replicate indices to run (for resuming or
for verification against archived replicates). Defaults to \code{1:B}.}

\item{stratum}{Name of the stratum variable in the data; permutation is
within its levels. \code{NULL} permutes across the whole sample.}

\item{covariates}{Character vector of covariate names for the Freedman-Lane
regression (typically the fullest covariate set). Required for \code{"fl"}.}

\item{transform, inverse}{Functions applied to the exposure before the
Freedman-Lane regression and to map back afterwards (default \code{log}/\code{exp},
appropriate for positive, right-skewed markers).}

\item{restrict}{Named list restricting subset and weighting axes for the
null, e.g. \code{list(missing = "imputed", weight = "design")}; the null is
then computed on that subgrid only. \code{NULL} uses the full grid.}

\item{seed}{Integer; replicate \code{b} uses \code{set.seed(seed + b)}.}

\item{dir}{Optional directory; each replicate is written to
\code{sprintf("rep_\%04d.csv", b)} there and skipped if the file exists, so long
jobs can be resumed.}

\item{cores}{Cores for \code{parallel::mclapply} over replicates.}

\item{data}{Data frame; required only for grids from \code{\link[=as_spec_grid]{as_spec_grid()}}.}

\item{keep}{Columns of the refitted grid to store per replicate.}
}
\value{
An object of class \code{spec_null}: a list with \code{perm} (data frame:
\code{rep}, \code{exposure}, axis columns, kept columns), \code{scheme}, \code{reps}, \code{seed},
\code{stratum}, \code{restrict}, \code{axis_cols}.
}
\description{
Refits the grid on permuted data to obtain, for every specification, the
distribution of estimates and P values under a null of no exposure-outcome
association. Two schemes are available:
}
\details{
\itemize{
\item \code{"fl"} (default): within-stratum Freedman-Lane permutation. Each
(transformed) exposure is regressed on the covariates in \code{covariates}; the
fitted part is kept and only the residuals are permuted within strata. This
removes the exposure-outcome association while preserving the association
of the exposure with the covariates, so the null is "no association given
the covariates", i.e. confounding is intact.
\item \code{"simple"}: within-stratum permutation of the exposure itself, which also
breaks the exposure-covariate association and therefore tests a null in
which confounding does not exist. It is anticonservative when the exposure
is confounded; it is provided for comparison.
}

All exposures in the grid are permuted jointly with the same permutation, so
their correlation structure is preserved. Every replicate is saved
specification by specification, which is what the calibration functions
need.
}
