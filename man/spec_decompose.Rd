% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_decompose.R
\name{spec_decompose}
\alias{spec_decompose}
\title{Which analytic choice moves the estimate: variance decomposition of a grid}
\usage{
spec_decompose(
  grid,
  null = NULL,
  scale = "estimate",
  keep = NULL,
  interactions = FALSE,
  equal_cardinality = NULL,
  axes = NULL
)
}
\arguments{
\item{grid}{A \code{spec_grid}.}

\item{null}{Optional \code{\link[=spec_null]{spec_null()}} (its \code{perm} must carry \code{estimate}).}

\item{scale}{Column to decompose: \code{"estimate"} (log odds ratio), \code{"lrr"}
(log marginal risk ratio, derived from \code{rr}) or \code{"rd"}. For the null only
\code{"estimate"} is available.}

\item{keep}{Optional logical vector selecting rows of \code{grid} (and the
matching null specifications).}

\item{interactions}{Logical; add pairwise interactions.}

\item{equal_cardinality}{\code{NULL} or \verb{list(axis = "<axis>", k = <levels>, max_combos = 500)}.}

\item{axes}{Axis columns to use (default: all axis columns with more than
one level among the selected rows).}
}
\value{
A list of class \code{spec_decompose} with \code{shares} (data frame: exposure,
term, share, df, mean_sq), \code{main} (wide table of axis shares per exposure),
\code{null} (per-exposure quantiles of each axis share under the null and the
observed share), \code{equal_cardinality} (per-exposure median and 5th-95th
percentiles of the shares of the reduced axis and of every other axis, and
the proportion of draws in which the reduced axis is the largest).
}
\description{
Partitions the variance of the specification-level estimates by analysis of
variance on the axes of the grid, separately for each exposure. The grid is
fully crossed, so sequential sums of squares do not depend on the order of
entry. The estimates are deterministic functions of one dataset, so this is
a descriptive partition and no F tests are attached.
}
\details{
Three refinements guard the interpretation:
\itemize{
\item \code{interactions = TRUE} adds all pairwise interactions, so the share of main
effects, pairwise interactions and higher-order terms can be reported.
\item \code{equal_cardinality} re-runs the decomposition on random draws of \code{k}
levels of one axis (all combinations when feasible), to check whether an
axis dominates merely because it has more levels.
\item \code{null} computes the same decomposition in every permutation replicate, so
the observed share of an axis can be compared with its distribution when
no association exists. A large share under the null means the axis
transmits confounding and sampling variability, not that it carries
evidence about the association.
}
}
