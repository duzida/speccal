% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_grid.R
\name{as_spec_grid}
\alias{as_spec_grid}
\title{Coerce a table of specification-level results into a \code{spec_grid}}
\usage{
as_spec_grid(
  df,
  axes,
  exposure = NULL,
  estimate = "estimate",
  se = "se",
  p = "p",
  refit = NULL
)
}
\arguments{
\item{df}{A data frame.}

\item{axes}{Character vector of column names that identify the axes.}

\item{exposure}{Column name identifying the exposure (or \code{NULL} for a single
exposure).}

\item{estimate, se, p}{Column names of the log-scale estimate, its standard
error and its P value.}

\item{refit}{Optional function \code{refit(newdata)} returning a \code{spec_grid} with
the same rows for new data; required by \code{\link[=spec_null]{spec_null()}}.}
}
\value{
A \code{spec_grid}.
}
\description{
Use this for results produced outside speccal (for example by \code{specr} or a
hand-written loop). The table needs one row per specification, columns
identifying the axis levels, and an estimate on the log scale with its
standard error and P value.
}
