% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composite_constraint.R
\name{composite_constraint}
\alias{composite_constraint}
\title{Test the fixed-ratio constraint implied by a composite index}
\usage{
composite_constraint(
  data,
  design,
  composite,
  constituents,
  axes,
  transform = log,
  free = NULL,
  alpha = 0.05,
  cores = 1L
)
}
\arguments{
\item{data}{Data frame.}

\item{design}{A \code{survey.design} on \code{data} (row order must match).}

\item{composite}{Name of the composite column.}

\item{constituents}{Named numeric vector of constituent column names and
their weights on the transformed scale, e.g. \code{c(NEU = 1, LYM = -1)}.
The identity \code{transform(composite) == sum(w * transform(constituent))} is
checked.}

\item{axes}{A \code{\link[=spec_axes]{spec_axes()}} object; its \code{outcome}, \code{covariates} and subset
axes define the specifications in which the constraint is tested (coding
and weighting axes are ignored: the composite enters on the transformed
scale, design-based).}

\item{transform}{Transformation under which the composite is linear in its
constituents (default \code{log}).}

\item{free}{Names of the constituents entered freely (default: all but the
last). The Wald statistic does not depend on which one is omitted.}

\item{alpha}{Significance level for the summary.}

\item{cores}{Cores for \code{parallel::mclapply} over specifications.}
}
\value{
A data frame of class \code{composite_constraint} with one row per
specification: axis columns, \code{k_free}, \code{F}, \code{p_constraint},
\code{p_composite} (P value of the composite in the model without the free
constituents), \code{sep_flag}; attribute \code{summary} (rejection rate, rejection
rate excluding levels named in \code{exclude} of \code{summary()}, composite
significance rate).
}
\description{
On the log scale a composite index such as a ratio or product of measured
quantities is a linear combination of its constituents with coefficients
fixed at +1 or -1 (for example log NLR = log N - log L). Entering the
composite in a regression therefore equals entering the constituents with
their coefficients constrained to that ratio. The constraint is testable:
the free constituents (all but one) are added to a model that already
contains the composite and tested jointly with a design-based Wald test.
Rejection means the data prefer a different weighting of the constituents
than the composite imposes. The test is a within-sample comparison of
nested models; a composite can fail it and still predict another outcome
well.
}
